#!/usr/bin/sh
[ "$1" = --wait-till-online ] && while ! timeout 2 curl -s google.com >/dev/null; do
    echo Waiting for network...
    sleep 1
done
electron ~/bin/node/node_modules/vieb/app --enable-features=UseOzonePlatform --ozone-platform=wayland "$@"
