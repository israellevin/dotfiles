#!/usr/bin/sh
[ "$1" = --wait-till-online ] && shift && while ! timeout 2 curl -s google.com >/dev/null; do
    echo Waiting for network...
    sleep 1
done
if [ "$1" ]; then
    qutebrowser --untrusted-args "$@"
    getwin.sh qutebrowser
else
    getwin.sh qutebrowser || qutebrowser
fi
